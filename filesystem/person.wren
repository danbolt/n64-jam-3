class Person {
  // Should return a string about the person
  brief() { _brief }
  brief=(value) { _brief = value }

  // Should return some lines when spoken to
  talkLines() { _description }
  talkLines=(value) { _description = value }
}